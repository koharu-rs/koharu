---
title: '格式与数据位置'
description: '查找项目、非秘密设置、凭据与可重新下载的缓存。'
icon: 'database'
---

## 支持格式

| 用途     | 格式                                |
| -------- | ----------------------------------- |
| 导入     | PNG、JPEG、WebP、CBZ、ZIP、RAR、PDF |
| 合并导出 | PNG                                 |
| 分层交换 | PSD                                 |
| 工作项目 | `.khrproj` 目录                     |

## 项目数据

项目位于 `Documents/Koharu/<project-name>.khrproj/`，包含交替写入的持久状态文件，以及保存源图、生成画面和手动栅格编辑的内容寻址 blob。

<Warning>项目打开时不要修改内部文件。在项目列表中删除会永久移除整个目录。</Warning>

## 设置与凭据

非秘密的类型化设置位于 `~/.koharu/config.toml`，各部分由流水线、翻译服务、排字和 Agent 管理。缺失字段使用默认值；无效的手动编辑可能导致初始化失败，请优先使用设置界面。

服务凭据使用操作系统安全凭据服务。项目备份不包含密钥或 Agent 账号会话。

## 运行时与模型缓存

软件包位于系统缓存中的 `<cache>/koharu/packages/`。关闭 Koharu 后移除缓存，会让需要的软件包重新下载。

桌面进程可能仍加载原生库时，不要删除相应软件包。

## 安全备份

<Steps>
  <Step title='关闭项目'>完成当前工作后再复制文件。</Step>
  <Step title='复制完整目录'>状态文件与 blob 必须一起保存。PSD 导出不是项目备份。</Step>
  <Step title='单独保存设置'>
    需要时复制 `config.toml`，并在恢复后的设备上通过 Koharu 重新配置凭据。
  </Step>
</Steps>
